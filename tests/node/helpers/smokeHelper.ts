// Trivial TypeScript helper imported by the smoke test.
// Its sole purpose is to prove the TS loader (tsx) resolves and runs .ts modules.
export function add(a: number, b: number): number {
  return a + b;
}
